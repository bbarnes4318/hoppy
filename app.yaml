name: hopwhistle
region: nyc

services:
  - name: api
    dockerfile_path: api/Dockerfile
    source_dir: api
    http_port: 8080
    instance_count: 1
    instance_size_slug: basic-xxs
    health_check:
      http_path: /healthz
    envs:
      - key: DATABASE_URL
        scope: RUN_TIME
        type: SECRET
      - key: JWT_SECRET
        scope: RUN_TIME
        type: SECRET
      - key: ENABLE_TIMESCALE
        scope: RUN_TIME
        value: "true"
      - key: CORS_ORIGINS
        scope: RUN_TIME
        value: '["https://hopwhistle-web.ondigitalocean.app"]'
      - key: ENVIRONMENT
        scope: RUN_TIME
        value: "production"
    routes:
      - path: /api

  - name: web
    dockerfile_path: web/Dockerfile
    source_dir: web
    http_port: 3000
    instance_count: 1
    instance_size_slug: basic-xxs
    health_check:
      http_path: /
    envs:
      - key: NEXT_PUBLIC_API_URL
        scope: RUN_TIME
        value: "https://hopwhistle-api.ondigitalocean.app"
      - key: NEXT_PUBLIC_WS_URL
        scope: RUN_TIME
        value: "wss://hopwhistle-api.ondigitalocean.app"
    routes:
      - path: /

databases:
  - name: hopwhistle-db
    engine: PG
    version: "15"
    production: true
